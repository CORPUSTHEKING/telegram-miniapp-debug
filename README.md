# Telegram Mini App Debug (GitHub Pages)

This repository hosts a minimal Telegram Mini App frontend for diagnosing
`initData` issues.

Purpose:
- Dump raw `Telegram.WebApp.initData`
- Dump `initDataUnsafe`
- Verify what Telegram actually sends before backend verification

Served via GitHub Pages (HTTPS, static, deterministic).
